<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="stock_quant_relocate_action" model="ir.actions.act_window">
        <field name="name">Relocate quants</field>
        <field name="res_model">stock.quant.relocate</field>
        <field name="binding_model_id" ref="stock.model_stock_quant" />
        <field name="view_mode">form</field>
        <field name="context" eval="{'location_origin_readonly': True}" />
        <field name="target">new</field>
        <field name="groups_id" eval="[(4, ref('stock.group_stock_user'))]" />
    </record>

<!--     <record id="view_stock_quant_tree_editable" model="ir.ui.view">
        <field name="name">stock.quant.list.editable</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_stock_quant_tree_editable" />
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_stock_quant_relocate']" position="attributes">
                <attribute name="name">marin.group_account_move_cancel</attribute>
            </xpath>
        </field>
    </record>
 --></odoo>
