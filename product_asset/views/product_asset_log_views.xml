<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_search_product_asset_log" model="ir.ui.view">
        <field name="name">product.asset.log.search</field>
        <field name="model">product.asset.log</field>
        <field name="arch" type="xml">
            <search string="Services Logs">
                <field name="asset_id" />
                <field name="product_id" />
                <field name="operator_id" />
                <field name="asset_manager_id" />
                <field name="vendor_id" />
                <separator />
                <filter name="inactive" string="Archived"
                    domain="[('active', '=', False)]" />
                <group>
                    <filter name="groupby_product_id" string="Service"
                        context="{'group_by': 'product_id'}" />
                    <filter name="groupby_product_category_id" string="Service Type"
                        context="{'group_by': 'product_category_id'}" />
                    <filter name="groupby_operator_id" string="Operator"
                        context="{'group_by': 'operator_id'}" />
                    <filter name="groupby_asset_manager_id" string="Asset Manager"
                        context="{'group_by': 'asset_manager_id'}" />
                    <filter name="groupby_vendor_id" string="Vendor"
                        context="{'group_by': 'vendor_id'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="view_list_product_asset_log" model="ir.ui.view">
        <field name="name">product.asset.log.list</field>
        <field name="model">product.asset.log</field>
        <field name="arch" type="xml">
            <list string="Logs" multi_edit="1" expand="1">
                <field name="company_id" column_invisible="True" />
                <field name="currency_id" column_invisible="True" />
                <field name="date" readonly="1" />
                <field name="asset_id" readonly="1" widget="many2one_avatar" />
                <field name="operator_id" readonly="1" widget="many2one_avatar" optional="show" />
                <field name="asset_manager_id" readonly="1" widget="many2one_avatar" optional="show" />
                <field name="vendor_id" optional="show" />
                <field name="product_category_id" optional="show" />
                <field name="product_id" optional="show" />
                <field name="qty_fuel" optional="hidden" />
                <field name="efficiency" optional="hidden" />
                <field name="amount" sum="Total" widget="monetary" optional="show" />
                <field name="odometer" optional="show" />
                <field name="state"
                    widget="badge"
                    readonly="1"
                    decoration-success="state == 'done'"
                    decoration-warning="state == 'new'"
                    decoration-info="state == 'running'" />
            </list>
        </field>
    </record>

    <record id="view_kanban_product_asset_log" model="ir.ui.view">
        <field name="name">product.asset.log.kanban</field>
        <field name="model">product.asset.log</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state">
                <field name="currency_id" />
                <progressbar field="activity_state"
                    colors='{"planned": "success", "today": "warning", "overdue": "danger"}' />
                <templates>
                    <t t-name="card">
                        <div class="flex-row">
                            <field name="asset_id" widget="image"
                                options="{'preview_image': 'image_128'}"
                                class="float-start col-2 pe-2" />
                            <div class="col-10 pe-2 text-truncate">
                                <field class="fw-bolder" name="asset_id" />
                                <span
                                    t-attf-class="float-end badge #{['todo', 'running'].indexOf(record.state.raw_value) > -1 ? 'text-bg-secondary' : ['cancelled'].indexOf(record.state.raw_value) > -1 ? 'text-bg-danger' : 'text-bg-success'}">
                                    <field name="state" />
                                </span>
                                <div class="text-truncate">
                                    <em>
                                        <field name="product_id" />
                                    </em>
                                </div>
                            </div>
                        </div>
                        <div class="text-truncate">
                            <field name="operator_id" />
                            <field class="float-end" name="date" />
                        </div>
                        <field class="text-truncate" name="vendor_id" />
                        <footer class="pt-0">
                            <field name="amount" widget="monetary"
                                options="{'currency_field': 'currency_id'}" />
                            <field name="activity_ids" widget="kanban_activity" class="ms-auto" />
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_graph_product_asset_log" model="ir.ui.view">
        <field name="name">product.asset.log.graph</field>
        <field name="model">product.asset.log</field>
        <field name="arch" type="xml">
            <graph string="Services Costs Per Month" sample="1">
                <field name="date" />
                <field name="asset_id" />
                <field name="amount" type="measure" />
            </graph>
        </field>
    </record>

    <record id="view_pivot_product_asset_log" model="ir.ui.view">
        <field name="name">product.asset.log.pivot</field>
        <field name="model">product.asset.log</field>
        <field name="arch" type="xml">
            <pivot>
                <field name="currency_id" invisible="1" />
                <field name="product_id" type="col" />
                <field name="vendor_id" type="row" />
                <field name="asset_id" type="row" />
                <field name="amount" type="measure" />
            </pivot>
        </field>
    </record>

    <record id="view_form_product_asset_log" model="ir.ui.view">
        <field name="name">product.asset.log.form</field>
        <field name="model">product.asset.log</field>
        <field name="arch" type="xml">
            <form string="Services Logs">
                <field name="active" invisible="1" />
                <field name="company_id" invisible="1" />
                <field name="currency_id" invisible="1" />
                <header>
                    <field name="state" widget="statusbar" options="{'clickable': '1'}" />
                </header>
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger"
                        invisible="active" />
                    <group>
                        <group string="Vendor">
                            <field name="vendor_id" widget="many2one_avatar" />
                            <field name="date" />
                            <field name="product_id" />
                        </group>
                        <group string="Vehicle">
                            <field name="asset_id" />
                            <field name="operator_id" widget="many2one_avatar" />
                            <field name="amount"
                                widget="monetary"
                                class="w-25"
                                help="Cost that is paid only once at the creation of the contract" />
                            <label for="odometer" />
                            <div class="o_row">
                                <field name="odometer" class="w-25" />
                            </div>
                        </group>
                    </group>
                    <group>
                        <group name="1" string="Contract">
                            <field name="date_start" />
                            <field name="date_end" />
                        </group>
                        <group name="2">
                        </group>
                    </group>
                    <separator string="Notes" />
                    <field name="notes" nolabel="1"
                        placeholder="Write here any other information related to the service completed." />
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="view_activity_product_asset_log" model="ir.ui.view">
        <field name="name">product.asset.log.activity</field>
        <field name="model">product.asset.log</field>
        <field name="arch" type="xml">
            <activity string="Services">
                <templates>
                    <div t-name="activity-box">
                        <img
                            t-att-src="activity_image('stock.lot', 'image_128', record.asset_id.raw_value)"
                            role="img" t-att-title="record.asset_id.value"
                            t-att-alt="record.asset_id.value" />
                        <div class="ms-2">
                            <field name="asset_id" class="o_text_block" />
                        </div>
                    </div>
                </templates>
            </activity>
        </field>
    </record>

    <record id="action_product_asset_log" model="ir.actions.act_window">
        <field name="name">Logs</field>
        <field name="res_model">product.asset.log</field>
        <field name="view_mode">list,kanban,form,graph,pivot,activity</field>
        <field name="context">{'search_default_groupby_product_id': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new service entry
            </p>
            <p>
                Track all the services done on your vehicle.
                Services can be of many types: occasional repair, fixed maintenance, etc.
            </p>
        </field>
    </record>

</odoo>