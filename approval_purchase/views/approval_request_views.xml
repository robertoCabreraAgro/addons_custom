<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="approval_purchase_request_view_form_inherit" model="ir.ui.view">
        <field name="name">approval.purchase.request.view.form.inherit</field>
        <field name="model">approval.request</field>
        <field name="inherit_id" ref="approvals.approval_request_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_get_attachment_view']" position="after">
                <button name="action_view_purchase_order"
                    type="object" class="oe_stat_button"
                    icon="fa-credit-card"
                    invisible="count_purchase_order == 0"
                    groups="purchase.group_purchase_user">
                    <field name="count_purchase_order" string="Purchase Orders"
                        widget="statinfo"
                        groups="purchase.group_purchase_user" />
                </button>
            </xpath>
            <xpath expr="//button[@name='action_confirm']" position="after">
                <button name="action_create_purchase_orders"
                    string="Create RFQ's"
                    type="object" class="btn-primary"
                    data-hotkey="g"
                    invisible="approval_type != 'purchase' or request_status != 'approved' or count_purchase_order &gt; 0"
                    groups="purchase.group_purchase_user" />
            </xpath>
        </field>
    </record>

</odoo>
